//Declaração de variáveis:

let valor = 50;
let dobro = 0;
let triplo = 0;

dobro = valor * 2;
triplo = valor * 3;

console.log("O dobro de: " + valor + " é " + dobro);
console.log("O triplo de: " + valor + " é " + triplo)

//Usamos console.log para o terminal e para o node, e no navegador usamos document.write
